name: CLA Validation

on:
  pull_request:

jobs:
  validate-cla:
    permissions:
      pull-requests: write
      issues: write
    uses: BOLL7708/reusable_workflows/.github/workflows/cla-validator.yaml@use
    secrets:
      TOKEN : ${{ secrets.GITHUB_TOKEN }}
